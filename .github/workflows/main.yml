name: Blog SSH CD
on:
  push:
    branches: [main]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: SSH and build
        uses: appleboy/ssh-action@v1
        with:
          host: 192.168.127.129
          username: trungma
          password: trungma
          port: 22
          script: |
            cd /opt/blog-ssh-cd/blog-ssh-cd
            git checkout main
            git pull
            npm i
            npm run build
            npm start
